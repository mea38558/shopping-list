# Operating System files
.DS_Store
Thumbs.db

# IDE and Editor files
.vscode/
.idea/
*.iml
.project
.classpath
.settings/

# General build artifacts and logs (often handled by sub-project .gitignore but good for redundancy)
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (already handled in sub-projects, but here for completeness if any existed at root)
.env
.env.*

# Node.js artifacts (already handled in sub-projects, but for any top-level occurrence)
node_modules/

# Build outputs (already handled in sub-projects, but for any top-level occurrence)
build/
dist/